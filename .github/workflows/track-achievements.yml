name: Track Project Achievements
on:
  schedule:
    - cron: '0 2 * * *'  # Run daily at 02:00 UTC (2 hours after deploy-selfhosted)
  workflow_dispatch:      # Allow manual trigger

permissions:
  contents: write
  actions: write

jobs:
  track-achievements:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 2  # Need at least 2 commits for git diff comparison

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Download latest report list and streak index
        run: |
          # Try to download streak-index.json first (compact, O(1) per project lookup)
          mkdir -p historical-data
          
          if wget -O historical-data/streak-index.json https://turbolego.github.io/uustreak/historical-data/streak-index.json 2>/dev/null; then
            echo "✓ Downloaded streak-index.json from published site"
          else
            echo "⚠ Could not download streak-index.json (may not exist yet)"
            rm -f historical-data/streak-index.json
          fi

          # Also download report-list.json as fallback (used if streak-index is absent)
          if wget -O historical-data/report-list.json https://turbolego.github.io/uustreak/historical-data/report-list.json 2>/dev/null; then
            echo "✓ Downloaded report-list.json from published site"
          else
            echo "⚠ Could not download report-list.json, will use local version if available"
            if [ ! -f historical-data/report-list.json ]; then
              echo "ℹ No local report-list.json found, creating empty list"
              echo "[]" > historical-data/report-list.json
            fi
          fi

      - name: Track achievements
        run: |
          echo "🏆 Running achievement tracking..."
          node js/track-achievements.js

      - name: Verify projects.json was updated
        run: |
          if grep -q "achievements" projects.json; then
            echo "✓ projects.json contains achievements data"
            # Show a sample (limit results in jq to avoid broken pipe)
            echo "Sample achievements entry:"
            jq '.[] | select(.achievements | length > 0) | {name: .name, achievements: .achievements} | limit(2; .)' projects.json
          else
            echo "ℹ No achievements found yet (this is normal for first run)"
          fi

      - name: Commit and push changes
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          
          # Check if there are changes
          if git diff --quiet projects.json; then
            echo "ℹ No changes to commit"
          else
            echo "📝 Committing updated achievements..."
            git add projects.json
            git commit -m "feat: update project achievements [skip ci]"
            git push origin main
            echo "✓ Changes pushed successfully"
          fi
        continue-on-error: true

      - name: Trigger deployment if projects.json changed
        if: always()
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const { execSync } = require('child_process');
            
            try {
              // Check if projects.json has changes
              let diff = '';
              try {
                diff = execSync('git diff HEAD~1 HEAD -- projects.json', { encoding: 'utf8' });
              } catch (e) {
                // If HEAD~1 doesn't exist (shallow clone), check if projects.json has achievements
                try {
                  diff = execSync('git diff HEAD -- projects.json', { encoding: 'utf8' });
                } catch (e2) {
                  console.log('ℹ Could not determine git diff');
                }
              }
              
              if (diff.includes('achievements')) {
                console.log('✓ Achievements were updated, triggering deployment...');
                
                // Trigger deploy-selfhosted or deploy workflow
                await github.rest.actions.createWorkflowDispatch({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  workflow_id: 'deploy.yml',
                  ref: 'main'
                });
                
                console.log('✓ Deployment workflow triggered');
              } else {
                console.log('ℹ No new achievements detected, skipping deployment');
              }
            } catch (error) {
              console.log('ℹ Error checking for updates:', error.message);
            }
